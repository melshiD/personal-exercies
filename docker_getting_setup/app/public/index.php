<?php

$name = 'David DaMelsh';
echo "Hello " . $name;

// $pdo = new PDO('mysql:dbname=dev;host=mysql', 'dbuser', 'iuDbuser', [PDO::ATTR_ERRMODE => PDO::ERRMODE_EXCEPTION]);

// $query = $pdo->query('SHOW VARIABLES like "version"');

// $row = $query->fetch();

// echo 'MySQL version:' . $row['Value'];